[project]
name = "tiergen-interfaces"
version = "0.0.1"
description = "Backend Protocols: Sensor, InfraBackend, AttributionBackend."
requires-python = ">=3.12"
dependencies = ["tiergen-core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tiergen"]
