[project]
name = "tiergen-impls-base"
version = "0.0.1"
description = "Implementation interfaces, manifest schema, entry-point registry."
requires-python = ">=3.12"
dependencies = ["tiergen-core", "tiergen-interfaces"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tiergen"]
