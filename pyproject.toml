[project]
name = "tiergen-workspace"
version = "0.0.0"
description = "Workspace root for tiergen. Not a package; see the members."
requires-python = ">=3.12"
dependencies = [
    "tiergen-core",
    "tiergen-protocols",
    "tiergen-interfaces",
    "tiergen-impls-base",
    "tiergen-check",
    "tiergen-cli",
]

[dependency-groups]
dev = [
    "commit-check",
    "hypothesis",
    "pre-commit",
    "pyright",
    "pytest",
    "ruff",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["core", "protocols", "interfaces", "impls/*", "check", "cli"]

[tool.uv.sources]
tiergen-core = { workspace = true }
tiergen-protocols = { workspace = true }
tiergen-interfaces = { workspace = true }
tiergen-impls-base = { workspace = true }
tiergen-check = { workspace = true }
tiergen-cli = { workspace = true }

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
include = ["core", "protocols", "interfaces", "impls", "check", "cli"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["core", "protocols", "interfaces", "impls/_base", "check", "cli"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "PT", "SIM", "RUF"]

[tool.ruff.format]
# The design doc's Python blocks are sketches, laid out by hand.
exclude = ["*.md"]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
testpaths = [
    "core/tests",
    "protocols/tests",
    "interfaces/tests",
    "impls/_base/tests",
    "check/tests",
    "cli/tests",
]
